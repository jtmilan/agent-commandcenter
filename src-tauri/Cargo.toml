[package]
name = "agent-commandcenter"
version = "0.2.0"
description = "ADE host — multi-harness agent operator"
authors = ["jtmilan"]
edition = "2021"

[lib]
name = "agent_commandcenter_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
